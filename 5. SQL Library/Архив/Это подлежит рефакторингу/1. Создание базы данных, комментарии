-- Демонстрация комментариев (предпочтительный вид для SQL)

# Комментарий

/* Комментарий
на несколько
строк
*/


/* INFO
> CREATE DATABASE имя_базы    |   Создает объект БД или саму базу БД
> DROP DATABASE имя_базы      |   Удаляет базу данных

> База Данных - это набор объектов разных типов.
> Объектами базы данных могут быть:
  > TABLE        |  таблицы (см.2)
  > FUNCTION     |  хранимые функции (см.14)
  > PROCEDURE    |  хранимые процедуры (см.14)
  > VIEWS        |  вьюхи (см.13)


Как работает сервер БД:
1. Получает текст
2. Делает синтаксический анализ
3. Строит план - компилирует (какие таблицы подключить и т.д)
4. Выполняет - передает результат.
*/

-- создать базу данных
CREATE DATABASE name1;        -- создать БД с именем name1
CREATE DATABASE `name 2`;     -- создать БД с именем name 2
                               --    ` - это апостроф (там где буква ё)
CREATE DATABASE name55;

-- создать БД, если она еще не существует
CREATE DATABASE IF NOT EXISTS name1;

-- удалить базу данных
DROP DATABASE `name 2`;

-- удалить базу данных, если она существует
DROP DATABASE IF EXISTS `name 2`;

-- говорю серверу: используй БД name1
 -- далее для доступа к ее таблицам мне не придется снова писать ее имя
 -- без USE:   name1.table;     с USE:   table;
 -- даже если я написал USE name1, я могу обратиться к другой базе, например, name2.table
USE name1;

-- вывести имена всех имеющихся БД
SHOW DATABASES;



























